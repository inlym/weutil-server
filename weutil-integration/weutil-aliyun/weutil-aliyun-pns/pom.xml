<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ===== 父项目配置：继承阿里云聚合模块 ===== -->
    <parent>
        <groupId>com.weutil</groupId>
        <artifactId>weutil-aliyun</artifactId>
        <version>${revision}</version>
    </parent>

    <!-- ===== 模块标识信息：定义当前模块的坐标和说明 ===== -->
    <artifactId>weutil-aliyun-pns</artifactId>

    <name>${project.artifactId}</name>
    <description>阿里云号码认证服务集成模块 - 提供短信认证验证码发送能力</description>

    <!-- ===== 模块依赖：声明当前模块所需的依赖项 ===== -->
    <dependencies>
        <!-- 核心共享模块 -->
        <dependency>
            <groupId>com.weutil</groupId>
            <artifactId>weutil-common</artifactId>
        </dependency>

        <!-- 阿里云公共模块 -->
        <dependency>
            <groupId>com.weutil</groupId>
            <artifactId>weutil-aliyun-core</artifactId>
        </dependency>

        <!-- 阿里云号码认证服务 SDK -->
        <dependency>
            <groupId>com.aliyun</groupId>
            <artifactId>dypnsapi20170525</artifactId>
        </dependency>
    </dependencies>

</project>
