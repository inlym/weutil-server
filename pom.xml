<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://maven.apache.org/POM/4.0.0"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ===== 项目基本坐标信息：定义父项目的 Maven 唯一标识 ===== -->
    <groupId>com.weutil</groupId>
    <artifactId>weutil-server</artifactId>
    <version>${revision}</version>

    <!-- ===== 项目打包方式：父项目作为聚合器使用 pom 打包 ===== -->
    <packaging>pom</packaging>

    <!-- ===== 子模块声明：控制参与构建与发布的模块列表 ===== -->
    <modules>
        <module>weutil-common</module>
        <module>weutil-system</module>
        <module>weutil-account</module>
        <module>weutil-bootstrap</module>
    </modules>

    <name>${project.artifactId}</name>
    <description>「小鸣助手」项目服务端部分代码</description>

    <!-- ===== Spring Boot 父项目：继承官方默认配置与依赖版本 ===== -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath />
    </parent>

    <!-- ===== 全局属性：集中管理版本号、编码及编译配置 ===== -->
    <properties>
        <!-- 项目版本号 -->
        <revision>1.0.0</revision>

        <!-- Maven 相关配置 -->
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- 第三方依赖版本号 -->
        <mybatis-flex.version>1.11.7</mybatis-flex.version>
        <flatten-maven-plugin.version>1.6.0</flatten-maven-plugin.version>
        <hikari.version>5.1.0</hikari.version>
        <guava.version>33.4.0-jre</guava.version>
        <logstash-logback-encoder.version>8.0</logstash-logback-encoder.version>
        <logbook.version>4.0.4</logbook.version>
        <spring-ai.version>2.0.0</spring-ai.version>
        <jsonschema-generator.version>5.0.0</jsonschema-generator.version>

        <!-- 构建开关 -->
        <skipTests>true</skipTests>
    </properties>

    <!-- ===== 公共依赖：所有模块强制继承的最小依赖集合 ===== -->
    <dependencies>
        <!-- Spring Boot 核心 Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!-- Micrometer Prometheus 注册表：将 Micrometer 指标以 Prometheus 格式暴露在 /actuator/prometheus 端点 -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <!-- AspectJ Weaver：Spring Boot 4 移除了 spring-boot-starter-aop，AOP 核心由 spring-context 提供，此处仅需 aspectjweaver -->
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjweaver</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <!-- Spring Boot 配置注解处理器 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- Spring Boot Test 起步依赖 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Spring AI Starters -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
        </dependency>

        <!-- MySQL 驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- MyBatis-Flex -->
        <dependency>
            <groupId>com.mybatis-flex</groupId>
            <artifactId>mybatis-flex-spring-boot4-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mybatis-flex</groupId>
            <artifactId>mybatis-flex-processor</artifactId>
            <scope>provided</scope>
        </dependency>
        <!-- JDBC starter：MyBatis-Flex boot4 starter 不再传递引入，需显式声明 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <!-- Hikari 备注（2025.09.04） -->
        <!-- Spring Boot 3.x 已自带 Hikari，正常是不需要单独引入的，但是不这么做，就会被 Mybatis-flex 报错，见下述链接说明 -->
        <!-- https://mybatis-flex.com/zh/faq.html#springboot-项目-启动报错-property-sqlsessionfactory-or-sqlsessiontemplate-are-required -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
        </dependency>

        <!-- JSON Schema Generator：LlmSchemaUtils 工具类所需依赖 -->
        <dependency>
            <groupId>com.github.victools</groupId>
            <artifactId>jsonschema-generator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.victools</groupId>
            <artifactId>jsonschema-module-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.victools</groupId>
            <artifactId>jsonschema-module-swagger-2</artifactId>
        </dependency>
    </dependencies>

    <!-- ===== 依赖版本管理：统一约束内部模块与第三方版本 ===== -->
    <dependencyManagement>
        <dependencies>
            <!-- === 内部模块依赖管理 === -->
            <dependency>
                <groupId>com.weutil</groupId>
                <artifactId>weutil-common</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.weutil</groupId>
                <artifactId>weutil-system</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.weutil</groupId>
                <artifactId>weutil-account-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.weutil</groupId>
                <artifactId>weutil-account-credential</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>com.weutil</groupId>
                <artifactId>weutil-bootstrap</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- === 第三方依赖版本管理 === -->

            <!-- MyBatis-Flex -->
            <dependency>
                <groupId>com.mybatis-flex</groupId>
                <artifactId>mybatis-flex-spring-boot4-starter</artifactId>
                <version>${mybatis-flex.version}</version>
            </dependency>
            <dependency>
                <groupId>com.mybatis-flex</groupId>
                <artifactId>mybatis-flex-processor</artifactId>
                <version>${mybatis-flex.version}</version>
            </dependency>

            <!-- Hikari 备注（2025.09.04） -->
            <!-- Spring Boot 3.x 已自带 Hikari，正常是不需要单独引入的，但是不这么做，就会被 Mybatis-flex 报错，见下述链接说明 -->
            <!--
            https://mybatis-flex.com/zh/faq.html#springboot-项目-启动报错-property-sqlsessionfactory-or-sqlsessiontemplate-are-required -->
            <dependency>
                <groupId>com.zaxxer</groupId>
                <artifactId>HikariCP</artifactId>
                <version>${hikari.version}</version>
            </dependency>

            <!-- Spring AI BOM -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- JSON Schema Generator：Spring AI 工具定义生成所需依赖 -->
            <dependency>
                <groupId>com.github.victools</groupId>
                <artifactId>jsonschema-generator</artifactId>
                <version>${jsonschema-generator.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.victools</groupId>
                <artifactId>jsonschema-module-jackson</artifactId>
                <version>${jsonschema-generator.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.victools</groupId>
                <artifactId>jsonschema-module-swagger-2</artifactId>
                <version>${jsonschema-generator.version}</version>
            </dependency>

            <!-- Guava -->
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>

            <!-- Logstash Logback Encoder -->
            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash-logback-encoder.version}</version>
            </dependency>

            <!-- Logbook -->
            <dependency>
                <groupId>org.zalando</groupId>
                <artifactId>logbook-spring</artifactId>
                <version>${logbook.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ===== 构建配置：声明构建周期内使用的插件 ===== -->
    <build>
        <plugins>
            <!-- 统一 revision 版本管理 -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>${flatten-maven-plugin.version}</version>
                <configuration>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                    <updatePomFile>true</updatePomFile>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- 编译插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>com.mybatis-flex</groupId>
                            <artifactId>mybatis-flex-processor</artifactId>
                            <version>${mybatis-flex.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- Surefire 测试插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skipTests>${skipTests}</skipTests>
                </configuration>
            </plugin>

            <!-- 源码打包插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
